export const API_PREFIX = '/v1';

export const ROUTES = {
  AUTH: `${API_PREFIX}/auth`,
  CARS: `${API_PREFIX}/cars`,
  GARAGE: `${API_PREFIX}/garage`,
  //   PAYMENTS: `${API_PREFIX}/payments`, example of adding another route
};